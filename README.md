# `constexpr` Containers

> "Prefer compile-time computations to run-time computations" \
> -- Someone (probably)

A C++14 library that implements some `constexpr` containers.

## Todo
- [ ] Spruce up CMake
- [ ] Add example usage in this file
- [ ] Generate doxygen documentation in `docs/`
- [ ] Set up CI and code coverage
